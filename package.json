{
  "name": "opencode-still-going",
  "version": "1.0.1",
  "description": "OpenCode plugin that automatically sends \"continue\" when the model stops after repeated retries - never after a natural completion or a manual abort.",
  "type": "module",
  "license": "MIT",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types test/match.test.ts"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "still-going",
    "retry",
    "ai-agent"
  ],
  "devDependencies": {
    "@opencode-ai/plugin": "^1.0.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
